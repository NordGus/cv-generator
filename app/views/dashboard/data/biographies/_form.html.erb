<%=
  form_for biography,
           url: dashboard_person_data_resume_biography_path(person, resume, biography),
           method: :patch do |form|
%>
  <%= form.text_area :content %>
  <%= form.submit %>
<% end %>