<%= turbo_stream.update "#{dom_id(@person)}_new_contact" do %>
  <%= render "form", person: @person, contact: ::Data::Contact.new %>
<% end %>

<%= turbo_stream.append "#{dom_id(@person)}_contacts" do %>
  <%= render "contact", person: @person, contact: @contact %>
<% end %>
