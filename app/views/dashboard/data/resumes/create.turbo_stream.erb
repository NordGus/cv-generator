<%= turbo_stream.update "#{dom_id(@person)}_new_resume" do %>
  <%= render "form", person: @person, resume: ::Data::Resume.new %>
<% end %>

<%= turbo_stream.append "#{dom_id(@person)}_resumes" do %>
  <%= render "resume", person: @person, resume: @resume %>
<% end %>