<!DOCTYPE html>
<html lang="<%= params[:locale] || I18n.default_locale %>">
  <head>
    <title>CV Generator | Dashboard </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="p-6 w-full">
    <div class="flex w-full items-stretch justify-around p-3 bg-gray-500 rounded">
      <%= menu_item "People", dashboard_people_path, params[:controller] == "dashboard/people" %>
      <%= menu_item "Companies", "#", params[:controller] == "dashboard/companies" %>
      <%= menu_item "Technologies", "#", params[:controller] == "dashboard/technologies" %>
    </div>
    <div class="grow">
      <%= yield %>
    </div>
  </body>
</html>
